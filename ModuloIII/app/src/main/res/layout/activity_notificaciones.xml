<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.doer.moduloiii.Notificaciones">

    <Button
        android:id="@+id/SendNotification"
        android:text="Enviar Notificacion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="5dp"/>

    <Button
        android:id="@+id/SendAlert"
        android:text="Dialogo Personalizado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/SendNotification"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dialogo Confirmacion"
        android:id="@+id/btnConfirmacion"
        android:layout_below="@+id/SendAlert"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dialogo de Alerta"
        android:id="@+id/btnAlerta"
        android:layout_below="@+id/btnConfirmacion"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dialogo de Seleccion"
        android:id="@+id/btnSeleccion"
        android:layout_below="@+id/btnAlerta"
        android:layout_alignRight="@+id/btnAlerta"
        android:layout_alignEnd="@+id/btnAlerta" />
</RelativeLayout>
